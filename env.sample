# Environment Configuration File
# ====================================
# This is a sample environment configuration file for this project.
# Copy this file to .env and modify the values according to your environment.
# Do not commit the .env file to version control.
#
# Notes:
# 1. For development, use the default SQLite configuration
# 2. For production, uncomment and configure the MySQL DATABASE_URL
# 3. MySQL Docker configuration is used in docker-compose.yaml
# 4. Make sure to use strong passwords and secure keys in production
# 5. Do not commit sensitive information (keys, passwords, etc.) to version control
# 6. It's recommended to use different configuration files for different environments
#    (development, testing, production)

# ============================================================================
# Django Configuration
# ============================================================================
# Django secret key for session encryption, etc.
SECRET_KEY="kxt=+vy=z4s_c6cy6vnu!v7d4f4y$hx0*!+cm(y^obo^yq0%da"

# Debug mode, set to False in production
DJANGO_DEBUG=False

# Allowed hostnames, comma-separated
ALLOWED_HOSTS=localhost,127.0.0.1

# CSRF trusted origins, comma-separated
CSRF_TRUSTED_ORIGINS='http://localhost,http://192.168.8.182:8000'

# Logging level
DJANGO_LOG_LEVEL=INFO

# Default admin user configuration
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=adminpassword

# Database Configuration
# =====================
# Option 1: Default SQLite Database (Recommended for development)
DATABASE_URL=sqlite:///db.sqlite3

# Option 2: MySQL Database Configuration
# Uncomment and configure the following for MySQL
# DATABASE_URL=mysql://mito:mitoPass@localhost:3306/mito

# MySQL Docker Configuration (Used in docker-compose.yaml)
# MYSQL_ROOT_PASSWORD=root_password
# MYSQL_USER=mito
# MYSQL_PASSWORD=mitoPass
# MYSQL_DATABASE=mito
# MYSQL_PORT=3306

# Celery Configuration
# =====================
# Celery Broker URL for task queue
CELERY_BROKER_URL=redis://redis:6379/0

# Celery Result Backend
CELERY_RESULT_BACKEND=redis://redis:6379/0

# Celery Log Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
CELERY_LOG_LEVEL=INFO

# Email Configuration
# =====================
# Email server host
# EMAIL_HOST=smtp.example.com

# Email server port
# EMAIL_PORT=587

# Email server username
# EMAIL_HOST_USER=your-email@example.com

# Email server password
# EMAIL_HOST_PASSWORD=your-email-password

# Use TLS for email
# EMAIL_USE_TLS=True

# Default sender email
# DEFAULT_FROM_EMAIL=your-email@example.com

# Social Authentication
# =====================
# Google OAuth Configuration
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_CALLBACK_URL=

# WeChat Configuration
# WECHAT_APP_ID=
# WECHAT_APP_SECRET=

# ============================================================================
# (Devtoolbox)LLM Configuration
# ============================================================================
# OPENAI_API_BASE=https://api.openai.com/v1/
# OPENAI_API_KEY=your_secret_key

# ============================================================================
# (Devtoolbox)Speech Processing Module Configuration
# ============================================================================
# Azure Speech Services Configuration
# Used for accessing Azure Speech services, providing speech-to-text and text-to-speech functionality
# AZURE_SPEECH_KEY=your-azure-speech-key
# AZURE_SPEECH_REGION=your-azure-speech-region

# Azure Storage configuration is required because audio files longer than
# one minute must be uploaded to S3-compatible storage before Azure can
# process them for speech recognition.
# AZURE_STORAGE_ACCOUNT=your-azure-storage-account
# AZURE_STORAGE_KEY=your-azure-storage-key

# ============================================================================
# (Devtoolbox)Object Storage
# ============================================================================
# OSS_BUCKET_NAME=your-bucket-name
# OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
# OSS_ACCESS_KEY_ID=your-access-key-id
# OSS_ACCESS_KEY_SECRET=your-access-key-secret
# OSS_REGION=cn-hangzhou
# OSS_USE_VIRTUAL_STYLE=False

# ============================================================================
# Proxy Configuration
# ============================================================================
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080